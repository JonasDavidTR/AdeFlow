<Window x:Class="DivisorComprovanteRepasse.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DivisorComprovanteRepasse"
        mc:Ignorable="d"
        Title="Divisor de Comprovante de Repasse"
        Height="600"
        Width="900"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="25">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Título e Manual -->
        <Grid Grid.Row="0"
            Margin="0,0,0,20">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
               Text="Divisor de Comprovante de Repasse"
               FontSize="26"
               FontWeight="Bold"
               VerticalAlignment="Center"/>

            <Button x:Name="BtnManual"
            Grid.Column="1"
            Content="📖 MANUAL DE USO"
            Height="35"
            FontWeight="Bold"
            Click="BtnManual_Click"/>
        </Grid>

        <!-- Seleção do PDF -->
        <Grid Grid.Row="1"
              Margin="0,0,0,15">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="TxtCaminhoPdf"
                     Grid.Column="0"
                     Height="35"
                     VerticalContentAlignment="Center"
                     IsReadOnly="True"
                     Margin="0,0,10,0"/>

            <Button x:Name="BtnSelecionarPdf"
                    Grid.Column="1"
                    Content="Selecionar PDF"
                    Height="35"
                    Click="BtnSelecionarPdf_Click"/>

        </Grid>

        <!-- Botões de processamento -->
        <StackPanel Grid.Row="2"
            Orientation="Horizontal"
            HorizontalAlignment="Left">

            <Button x:Name="BtnLerPdf"
            Content="LER PDF"
            Width="180"
            Height="40"
            FontWeight="Bold"
            Margin="0,0,20,0"
            Click="BtnLerPdf_Click"/>

            <Button x:Name="BtnDividirPdf"
            Content="DIVIDIR PDF"
            Width="180"
            Height="40"
            FontWeight="Bold"
            Margin="0,0,20,0"
            IsEnabled="False"
            Click="BtnDividirPdf_Click"/>

            <Button x:Name="BtnAbrirPastaPdf"
            Content="ABRIR PASTA"
            Width="150"
            Height="40"
            FontWeight="Bold"
            IsEnabled="False"
            Click="BtnAbrirPasta_Click"/>

        </StackPanel>





        <!-- Resultados -->
        <Grid Grid.Row="3"
              Margin="0,20,0,0">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="TxtStatus"
                       Grid.Row="0"
                       Text="Selecione um PDF para começar."
                       FontSize="15"
                       Margin="0,0,0,10"/>

            <DataGrid x:Name="GridResultados"
                      Grid.Row="1"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False">

                <DataGrid.Columns>

                    <DataGridTextColumn Header="Página"
                                        Binding="{Binding NumeroPagina}"
                                        Width="80"/>

                    <DataGridTextColumn Header="Cadastro"
                                        Binding="{Binding Cadastro}"
                                        Width="130"/>

                    <DataGridTextColumn Header="Nome do Cooperado"
                                        Binding="{Binding Nome}"
                                        Width="*"/>

                    <DataGridTextColumn Header="Competência"
                                        Binding="{Binding Competencia}"
                                        Width="120"/>

                </DataGrid.Columns>

            </DataGrid>

        </Grid>

    </Grid>

</Window>